BLOOD DONATION PLATFORM - SOURCE OF TRUTH

1. Redcross and WHO are source-system services; backend must not embed their extraction logic.
2. ETL is the integration layer that extracts from source systems and normalizes records for downstream stores.
3. Latitude and longitude resolution from pincode happens in ETL, not in source-system APIs.
4. ETL must load both blood bank and donor datasets into Elasticsearch and ClickHouse.
5. Backend operational blood-bank lookup APIs use Elasticsearch, not ClickHouse.
6. ClickHouse is reserved for analytics/reporting and Grafana-facing use cases.
7. Nearest blood bank lookup uses Elasticsearch native geo distance sort and returns max 20 records.
8. Elasticsearch bank and donor indexes must keep location as geo_point.
9. ETL startup must ensure Elasticsearch templates/mappings are created before indexing records.
10. Missing pincode mappings must degrade safely to default coordinates and continue ETL processing.
11. Public API contracts must be typed DTO/entity contracts, not Map-based request/response payloads.
12. Service/controller method signatures should prefer typed objects over generic map payloads.
13. API error responses must remain typed DTO responses.
14. PostgreSQL remains system-of-record for users, searches, requests, and responses.
15. Redcross and WHO repository SQL must be defined as class-level final constants, not inline per-function query strings.
16. Redcross and WHO DTO classes must use Lombok for boilerplate reduction and consistency.
17. Elasticsearch backend integration must use a class-level query template and typed response model mapping.
18. Deterministic behavior and fail-fast validation are mandatory for invalid API inputs.
19. Internal ETL in-memory state may use Map structures where required for merge/index operations.
20. Map usage is acceptable for internal ETL storage mechanics, but not for external API contracts.

RUNBOOK FOR NEXT COPILOT AGENT (validated on 2026-04-15)

21. Bring up the stack from repo root with `docker compose up -d --build` and verify with `docker compose ps`.
22. Generate dummy source data and CSV preview with `python db-seeder/seed.py`.
23. Confirm CSV overwrite behavior with 101 lines total (1 header + 100 rows): `Get-Content db-seeder/generated_seed_preview.csv | Measure-Object -Line`.
24. Trigger ETL bulk load manually (bulk is not automatic): `GET http://localhost:8083/admin/etl/bulk-load`.
25. Re-triggering the same API should return the idempotent response `bulk load already completed`.
26. Validate Elasticsearch ingestion using container-local calls:
	- `docker exec hemo-elasticsearch curl -s http://localhost:9200/donor_availability_current/_count`
	- `docker exec hemo-elasticsearch curl -s http://localhost:9200/bb_inventory_current/_count`
27. Validate ClickHouse ingestion:
	- `docker exec hemo-clickhouse clickhouse-client --query "SELECT count() FROM blood_ops.dim_donor"`
	- `docker exec hemo-clickhouse clickhouse-client --query "SELECT count() FROM blood_ops.dim_blood_bank"`
28. Use CSV-driven donor validation (read blood group + pincode from `generated_seed_preview.csv` donor rows) and call `GET /donors/search` on backend; expected `totalMatched > 0`.
29. Grafana validation path:
	- Open `http://localhost:3000/login`.
	- Login with `admin/admin`.
	- If prompted, click Skip password update for local validation.
	- Confirm landing on Grafana home and verify datasources via `GET /api/datasources` (basic auth admin/admin) includes Elasticsearch and ClickHouse.
30. Auth OTP behavior for local runs is deterministic: use OTP `123456`; wrong OTP returns HTTP 401 with body `{"error":"Invalid OTP"}`.
31. CSV-driven UI blood-bank validation uses data from `db-seeder/generated_seed_preview.csv`; one verified combination is pincode `144415`, group `O+`, component `Whole Blood`, which returns Redcross records in the first tab.
32. CSV-driven donor validation uses donor rows from `db-seeder/generated_seed_preview.csv`; one verified combination is blood group `O+` and pincode `151210`, and backend `GET /donors/search` returns `totalMatched > 0`.
33. Logging should stay minimal and controller-focused: log only important API triggers/results and exceptions at the controller/advice boundary; avoid service-level request logging unless it is part of ETL trigger visibility.
